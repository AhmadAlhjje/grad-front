# منصة قياس الأثر المجتمعي - Social Impact Platform

منصة متكاملة لقياس وتقييم الأثر المجتمعي للمبادرات والمشاريع.

## المميزات

- 📊 لوحة تحكم تفاعلية مع KPIs ومخططات بيانية
- 📁 إدارة المشاريع والمبادرات
- 📝 إنشاء وإدارة الاستبيانات (قبلي / بعدي / دراسة احتياج)
- 📈 تحليل البيانات وعرض الرؤى
- 👥 إدارة المستخدمين والصلاحيات
- 🌐 دعم اللغة العربية (RTL) والإنجليزية

## التقنيات المستخدمة

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: TailwindCSS
- **UI Components**: Headless UI
- **State Management**: Zustand + React Query
- **Forms**: React Hook Form + Zod
- **Charts**: Recharts
- **HTTP Client**: Axios
- **i18n**: next-intl

## هيكل المشروع

```
src/
├── api/                    # طبقة API (خدمات الـ Backend)
│   ├── auth.api.ts
│   ├── projects.api.ts
│   ├── surveys.api.ts
│   ├── responses.api.ts
│   ├── analysis.api.ts
│   └── users.api.ts
├── app/                    # Next.js App Router
│   ├── (auth)/            # صفحات المصادقة
│   │   ├── login/
│   │   ├── register/
│   │   └── forgot-password/
│   ├── (dashboard)/       # صفحات لوحة التحكم
│   │   ├── dashboard/
│   │   ├── projects/
│   │   ├── surveys/
│   │   ├── analysis/
│   │   └── users/
│   ├── layout.tsx
│   └── page.tsx
├── components/            # المكونات المشتركة
│   ├── charts/           # مكونات الرسوم البيانية
│   ├── guards/           # حراسة المصادقة والصلاحيات
│   ├── layout/           # مكونات التخطيط
│   └── ui/               # مكونات واجهة المستخدم
├── features/             # الوحدات حسب الميزة
│   └── surveys/
│       └── components/
├── hooks/                # Custom Hooks
├── i18n/                 # ملفات الترجمة
│   ├── config.ts
│   └── messages/
├── lib/                  # المكتبات والأدوات
│   ├── axios.ts
│   ├── query-client.ts
│   └── utils.ts
├── store/                # Zustand Stores
│   ├── auth.store.ts
│   └── ui.store.ts
└── types/                # TypeScript Types
    └── index.ts
```

## التثبيت

```bash
# تثبيت الاعتماديات
npm install

# تشغيل بيئة التطوير
npm run dev

# بناء المشروع
npm run build

# تشغيل الإنتاج
npm start
```

## متغيرات البيئة

قم بإنشاء ملف `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
NEXT_PUBLIC_APP_NAME=منصة قياس الأثر المجتمعي
```

## الصفحات الرئيسية

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| لوحة التحكم | `/dashboard` | عرض KPIs والمخططات |
| المشاريع | `/projects` | إدارة المشاريع |
| تفاصيل المشروع | `/projects/[id]` | عرض تفاصيل المشروع |
| الاستبيانات | `/surveys` | إدارة الاستبيانات |
| تعبئة الاستبيان | `/surveys/[id]/fill` | تعبئة الاستبيان |
| التحليل | `/analysis` | عرض نتائج التحليل |
| المستخدمون | `/users` | إدارة المستخدمين (مدير فقط) |

## نظام الأدوار

- **Admin (مدير)**: صلاحيات كاملة
- **Manager (مشرف)**: إدارة المشاريع والاستبيانات
- **Viewer (مشاهد)**: عرض فقط

## API Integration

جميع APIs متوافقة مع Postman Collection المقدمة:

- **Authentication**: `/auth/login`, `/auth/register`, `/auth/profile`
- **Projects**: `/projects`, `/projects/:id`, `/projects/:id/statistics`
- **Surveys**: `/surveys`, `/surveys/:id`, `/surveys/:id/questions`
- **Responses**: `/surveys/responses`, `/surveys/:id/responses`
- **Analysis**: `/analysis/survey-responses`, `/analysis/impact-evaluation`

## الترخيص

جميع الحقوق محفوظة © 2024
